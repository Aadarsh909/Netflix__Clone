### The steps to upload a file to git

- git init
- git add .
- git commit -am "initial commit"
- git remote add "repo-address"
- git push origin master
- git add 'file_name'
- git commit -m "Your commit message"
- git push origin master
